MODEL_TABLE_PATH = r"models/table_detector_v1.pt"

WINDOW_NAME = "PokerBot Live (press q to quit)"

CONF_THRES = 0.35
DEVICE = "cpu"          # keep cpu for now since your local CUDA is blocked
CAPTURE_FPS = 30
CAPTURE_REGION = None   # optionally reduce load: (left, top, right, bottom)
